# Disable directory browsing
Options -Indexes

# ----------------------------------------------------------------------
<<<<<<< HEAD
# Security Rules - Block XAMPP Dashboard Access
# ----------------------------------------------------------------------

RewriteEngine On

# Block XAMPP Dashboard Access - Redirect to login
# This blocks /dashboard, /dashboard/, /dashboard/index.php, etc. at root level
# But allows /ITE311-SIGNAR/admin/dashboard, /ITE311-SIGNAR/student/dashboard, /ITE311-SIGNAR/teacher/dashboard
RewriteCond %{REQUEST_URI} ^/(xampp|dashboard|webalizer|security|licenses|application)(/.*)?$ [NC]
RewriteCond %{REQUEST_URI} !^/ITE311-SIGNAR/(admin|student|teacher)/dashboard [NC]
RewriteCond %{REQUEST_URI} !^/ITE311-SIGNAR/ [NC]
RewriteRule ^.*$ /ITE311-SIGNAR/login [R=301,L]
=======
# Security Rules - Block Directory Traversal Attacks
# ----------------------------------------------------------------------

# Block access to any path containing .. (directory traversal)
RewriteEngine On

# Block directory traversal attempts - Multiple patterns
RewriteCond %{REQUEST_URI} (\.\./|\.\.\\|%2e%2e%2f|%2e%2e%5c|\.\.%2f|\.\.%5c) [NC]
RewriteRule ^.*$ /ITE311-SIGNAR/ [R=301,L]

# Block access to parent directories
RewriteCond %{REQUEST_URI} ^/\.\./ [NC]
RewriteRule ^.*$ /ITE311-SIGNAR/ [R=301,L]
>>>>>>> c3cd521911bcd31f5d0997904ea5026bc1bd85f7

# Block access to sensitive files and directories
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|inc|bak|sql|conf)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Block access to hidden files and directories
<FilesMatch "^\.">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Block access to system directories
RedirectMatch 403 ^/(app|system|vendor|writable|tests)/.*

# ----------------------------------------------------------------------
# Rewrite engine
# ----------------------------------------------------------------------

# Turning on the rewrite engine is necessary for the following rules and features.
# FollowSymLinks must be enabled for this to work.
<IfModule mod_rewrite.c>
	Options +FollowSymlinks
	RewriteEngine On

	# If you installed CodeIgniter in a subfolder, you will need to
	# change the following line to match the subfolder you need.
	# http://httpd.apache.org/docs/current/mod/mod_rewrite.html#rewritebase
	RewriteBase /ITE311-SIGNAR/

<<<<<<< HEAD
	# Security: Block XAMPP Dashboard Access
	# Block /dashboard, /dashboard/, /dashboard/index.php, etc. at root level
	# But allow /ITE311-SIGNAR/admin/dashboard, /ITE311-SIGNAR/student/dashboard, /ITE311-SIGNAR/teacher/dashboard
	RewriteCond %{REQUEST_URI} ^/(xampp|dashboard|webalizer|security|licenses|application)(/.*)?$ [NC]
	RewriteCond %{REQUEST_URI} !^/ITE311-SIGNAR/(admin|student|teacher)/dashboard [NC]
	RewriteRule ^.*$ /ITE311-SIGNAR/login [R=301,L]
=======
	# Security: Block directory traversal attempts - Multiple patterns
	RewriteCond %{REQUEST_URI} (\.\./|\.\.\\|%2e%2e%2f|%2e%2e%5c|\.\.%2f|\.\.%5c) [NC]
	RewriteRule ^.*$ /ITE311-SIGNAR/ [R=301,L]

	# Security: Block access to sensitive paths
	RewriteCond %{REQUEST_URI} ^/(app|system|vendor|writable|tests|\.git)/ [NC]
	RewriteRule ^.*$ /ITE311-SIGNAR/ [R=301,L]

	# Security: Block access to parent directories
	RewriteCond %{REQUEST_URI} ^/\.\./ [NC]
	RewriteRule ^.*$ /ITE311-SIGNAR/ [R=301,L]
>>>>>>> c3cd521911bcd31f5d0997904ea5026bc1bd85f7

	# Redirect Trailing Slashes...
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteCond %{REQUEST_URI} (.+)/$
	RewriteRule ^ %1 [L,R=301]

	# Rewrite "www.example.com -> example.com"
	RewriteCond %{HTTPS} !=on
	RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
<<<<<<< HEAD
	RewriteRule ^ https://localhost/ITE311-SIGNAR/{REQUEST_URI} [R=301,L]
=======
	RewriteRule ^ http://%1%{REQUEST_URI} [R=301,L]
>>>>>>> c3cd521911bcd31f5d0997904ea5026bc1bd85f7

	# Checks to see if the user is attempting to access a valid file,
	# such as an image or css document, if this isn't true it sends the
	# request to the front controller, index.php
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule ^([\s\S]*)$ index.php/$1 [L,NC,QSA]

	# Ensure Authorization header is passed along
	RewriteCond %{HTTP:Authorization} .
	RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
</IfModule>

<IfModule !mod_rewrite.c>
	# If we don't have mod_rewrite installed, all 404's
	# can be sent to index.php, and everything works as normal.
	ErrorDocument 404 index.php
</IfModule>

# Disable server signature start
ServerSignature Off
# Disable server signature end
